using System;
using System.Collections.Generic;
namespace practices
{
    class Program
    {
    static void Main(string[] args)
        {Dictionary<string,sting> profile = new Dictionary<string, string>();
        profile.Add("Name","Todd");
        profile.Add("Location", "Seattle");
        //loop through dictionay key/value pairs and print the key and fhe value for each (var item in profile)
        {
            Console.WriteLine("Key: {0}, Value: {1}", item.Key, item.Value);
        }
        }
    }
}
